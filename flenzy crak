--[[
                        KD5 HUB PROJECT
             Ready-to-Use (No Key & Fixed Version)
]]--

local Scripts = {
    [994732206]   = "https://raw.githubusercontent.com/realredz/BloxFruits/main/Source.lua", -- Blox Fruits
    [10004244222] = "https://raw.githubusercontent.com/realredz/BloxFruits/main/Source.lua", 
    [9792947201]  = "https://raw.githubusercontent.com/realredz/BloxFruits/main/Source.lua", 
    [10200395747] = "https://raw.githubusercontent.com/realredz/BloxFruits/main/Source.lua", 
}

local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local LocalPlayer = Players.LocalPlayer
local Mouse = LocalPlayer:GetMouse()
local gameId = game.GameId

local function Tween(obj, props, t, style, dir)
    style = style or Enum.EasingStyle.Quint
    dir = dir or Enum.EasingDirection.Out
    TweenService:Create(obj, TweenInfo.new(t, style, dir), props):Play()
end

local function Protect(gui)
    local env = (getgenv and getgenv()) or _G
    if env.HIDEUI then
        gui.Parent = env.HIDEUI
    elseif gethui then
        gui.Parent = gethui()
    elseif syn and syn.protect_gui then
        syn.protect_gui(gui)
        gui.Parent = game:GetService("CoreGui")
    else
        gui.Parent = game:GetService("CoreGui")
    end
end

local function New(class, props, parent)
    local inst = Instance.new(class)
    for k, v in pairs(props) do
        if k ~= "Children" and k ~= "Parent" then
            pcall(function() inst[k] = v end)
        end
    end
    if props.Children then
        for _, c in ipairs(props.Children) do
            pcall(function() c.Parent = inst end)
        end
    end
    inst.Parent = props.Parent or parent
    return inst
end

local function CircleRipple(btn, mx, my)
    task.spawn(function()
        btn.ClipsDescendants = true
        local nx = mx - btn.AbsolutePosition.X
        local ny = my - btn.AbsolutePosition.Y
        local sz = math.max(btn.AbsoluteSize.X, btn.AbsoluteSize.Y) * 1.6
        local c = New("ImageLabel", {
            Name = "Ripple",
            Image = "rbxassetid://266543268",
            ImageColor3 = Color3.fromRGB(255, 255, 255),
            ImageTransparency = 0.82,
            BackgroundTransparency = 1,
            ZIndex = btn.ZIndex + 5,
            Size = UDim2.new(0, 0, 0, 0),
            Position = UDim2.new(0, nx, 0, ny),
        }, btn)
        Tween(c, { Size = UDim2.new(0, sz, 0, sz), Position = UDim2.new(0.5, -sz/2, 0.5, -sz/2) }, 0.45, Enum.EasingStyle.Quad)
        Tween(c, { ImageTransparency = 1 }, 0.45, Enum.EasingStyle.Linear)
        task.wait(0.46)
        c:Destroy()
    end)
end

local function LoadScript()
    local url = Scripts[gameId] or Scripts[994732206]
    if not url or url == "" then
        warn("[KD5 Hub] Chưa có link script cho GameId này!")
        return
    end
    local ok, err = pcall(function() loadstring(game:HttpGet(url))() end)
    if not ok then 
        warn("[KD5 Hub] Lỗi tải script: " .. tostring(err)) 
    end
end

local function ShowUI()
    local SG = Instance.new("ScreenGui")
    SG.Name = "KD5_" .. tostring(math.random(1e6))
    SG.ZIndexBehavior = Enum.ZIndexBehavior.Global
    SG.ResetOnSpawn = false
    SG.IgnoreGuiInset = true
    Protect(SG)

    local Backdrop = New("Frame", {
        BackgroundColor3 = Color3.fromRGB(0, 0, 0),
        BackgroundTransparency = 1,
        BorderSizePixel = 0,
        Size = UDim2.new(1, 0, 1, 0),
        ZIndex = 200,
        Parent = SG,
    })

    local W, H = 400, 180
    local Card = New("Frame", {
        AnchorPoint = Vector2.new(0.5, 0.5),
        Position = UDim2.new(0.5, 0, 0.5, 0),
        Size = UDim2.new(0, W * 0.5, 0, H * 0.5),
        BackgroundColor3 = Color3.fromRGB(10, 8, 20),
        BackgroundTransparency = 0.80,
        BorderSizePixel = 0,
        ZIndex = 201,
        ClipsDescendants = true,
        Parent = SG,
        Children = {
            New("UICorner", { CornerRadius = UDim.new(0, 14) }),
            New("UIStroke", {
                Color = Color3.fromRGB(140, 60, 255),
                Transparency = 0.30,
                Thickness = 1,
                ApplyStrokeMode = Enum.ApplyStrokeMode.Border,
            }),
        }
    })

    local Header = New("Frame", {
        BackgroundColor3 = Color3.fromRGB(15, 10, 28),
        BorderSizePixel = 0,
        Size = UDim2.new(1, 0, 0, 44),
        ZIndex = 202,
        Parent = Card,
        Children = {
            New("UICorner", { CornerRadius = UDim.new(0, 14) }),
        }
    })

    New("TextLabel", {
        BackgroundTransparency = 1,
        Position = UDim2.new(0, 15, 0, 0),
        Size = UDim2.new(1, -50, 1, 0),
        Font = Enum.Font.FredokaOne,
        Text = "KD5 HUB — READY",
        TextColor3 = Color3.fromRGB(220, 180, 255),
        TextSize = 15,
        TextXAlignment = Enum.TextXAlignment.Left,
        ZIndex = 203,
        Parent = Header
    })

    local CloseBtn = New("ImageButton", {
        BackgroundTransparency = 1,
        AnchorPoint = Vector2.new(1, 0.5),
        Position = UDim2.new(1, -12, 0.5, 0),
        Size = UDim2.new(0, 20, 0, 20),
        Image = "rbxassetid://79324227570635",
        ImageColor3 = Color3.fromRGB(220, 80, 80),
        ZIndex = 203,
        Parent = Header
    })

    New("TextLabel", {
        BackgroundTransparency = 1,
        Position = UDim2.new(0, 15, 0, 55),
        Size = UDim2.new(1, -30, 0, 40),
        Font = Enum.Font.GothamBold,
        Text = "✓ Đã gộp thành công! Bấm kích hoạt để chạy\nscript mượt mà không cần nhập Key.",
        TextColor3 = Color3.fromRGB(130, 240, 160),
        TextSize = 12,
        TextWrapped = true,
        TextXAlignment = Enum.TextXAlignment.Center,
        ZIndex = 202,
        Parent = Card
    })

    local LaunchBtn = New("TextButton", {
        BackgroundColor3 = Color3.fromRGB(120, 50, 220),
        BorderSizePixel = 0,
        Position = UDim2.new(0, 15, 0, 110),
        Size = UDim2.new(1, -30, 0, 45),
        AutoButtonColor = false,
        Text = "",
        ZIndex = 202,
        Parent = Card,
        Children = {
            New("UICorner", { CornerRadius = UDim.new(0, 8) }),
            New("TextLabel", {
                BackgroundTransparency = 1,
                Size = UDim2.new(1, 0, 1, 0),
                Font = Enum.Font.FredokaOne,
                Text = "KÍCH HOẠT SCRIPT",
                TextColor3 = Color3.fromRGB(255, 255, 255),
                TextSize = 14,
                ZIndex = 203
            })
        }
    })

    local function AnimateClose()
        Tween(Card, { Size = UDim2.new(0, W * 0.65, 0, H * 0.65), BackgroundTransparency = 1 }, 0.2, Enum.EasingStyle.Quint, Enum.EasingDirection.In)
        Tween(Backdrop, { BackgroundTransparency = 1 }, 0.2, Enum.EasingStyle.Quint)
        task.delay(0.22, function() SG:Destroy() end)
    end

    CloseBtn.MouseButton1Click:Connect(AnimateClose)

    LaunchBtn.MouseButton1Click:Connect(function()
        CircleRipple(LaunchBtn, Mouse.X, Mouse.Y)
        AnimateClose()
        task.wait(0.25)
        LoadScript()
    end)

    Tween(Card, { Size = UDim2.new(0, W, 0, H), BackgroundTransparency = 0 }, 0.35, Enum.EasingStyle.Back, Enum.EasingDirection.Out)
    Tween(Backdrop, { BackgroundTransparency = 0.45 }, 0.35, Enum.EasingStyle.Quad)
end

ShowUI()
